{
  "name": "fiche-aep",
  "version": "1.0.0",
  "description": "Fiche Décisionnelle AEP — Application Android",
  "main": "index.js",
  "scripts": {
    "sync":  "npx cap sync",
    "open":  "npx cap open android",
    "build": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core":    "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
